bind status P !git push
bind status R !git pull --rebase
bind status A !@git add %(directory)

set commit-order = topo         # Order commits topologically
set horizontal-scroll = 33%     # Scroll 33% of the view width
